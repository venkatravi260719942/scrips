#!/bin/sudo bash

git --version
python3 --version
pip --version
docker --version
java --version 
javac --version
cat ~/.aws/credentials
wget --version
mvn --version
sonar-scanner --version
which sonar-scanner
which mvn
hostname